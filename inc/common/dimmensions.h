#ifndef DIMMENSIONS_H
#define DIMMENSIONS_H

const int windowWidth = 600;
const int windowHeight = 700;
const int circleRadius = 16;
const int platformWidth = 80;
const int platformHeight = 14;
const int borderSize = 10;

const float windowWidthf = static_cast<float>(windowWidth);
const float windowHeightf = static_cast<float>(windowHeight);
const float circleRadiusf = static_cast<float>(circleRadius);
const float platformWidthf = static_cast<float>(platformWidth);
const float platformHeightf = static_cast<float>(platformHeight);
const float borderSizef = static_cast<float>(borderSize);

#endif // DIMMENSIONS_H
